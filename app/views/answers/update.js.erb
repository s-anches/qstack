<% if @answer.errors.present? %>
  <%= render "layouts/error", errors: @answer.errors %>
<% else %>
  <% if remotipart_submitted? %>
    $('div#answer-<%= @answer.id %>').html("<%= j "#{render @answer}" %>");
  <% else %>
    $('div#answer-<%= @answer.id %>').html('<%= j render @answer %>');
  <% end %>
  $('form#edit_answer_<%= @answer.id %>').hide();
  bindClickEditAnswer();
  bindClickClose();
  bindAddFiles();
  bindPlaceFiles();
<% end %>
